from typing import Union

def square(x: Union[int, float]) -> Union[int, float]:
    """
    Calculates the square of a number.

    Args:
        x: The number to square (integer or float).

    Returns:
        The square of the input number (integer or float).
    """
    return x * x